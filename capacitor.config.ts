import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.onezite.wentzelstorage',
  appName: 'WentzelStorage',
  webDir: 'dist'
};

export default config;
